# AICouncil

适用于 [OpenCode](https://github.com/sst/opencode) 的多模型 AI 协作讨论插件。

## 概述

AICouncil 允许多个 AI 模型在 OpenCode 中参与协作讨论。支持：

- **多模型讨论** - Kimi、MiniMax、Claude、GPT-4o 等
- **主持人-参与者架构** - 一个模型担任主持人，其他模型参与讨论
- **轮次式讨论** - 结构化的对话流程
- **MCP 和 Skills 支持** - 完全兼容 OpenCode 的 MCP 和 Skills
- **国际化** - 支持中英文

## 安装

```bash
# 在你的 OpenCode 项目中
bun add @aicouncil/opencode-plugin
```

## 配置

### OpenCode 配置

将插件添加到你的 OpenCode 配置中：

```json
// ~/.opencode/config.json
{
  "plugins": ["@aicouncil/opencode-plugin"],
  "provider": {
    "kimi": {
      "options": {
        "baseURL": "https://api.kimi.com/coding/",
        "apiKey": "your-kimi-api-key"
      },
      "models": {
        "kimi-for-coding": {
          "name": "Kimi For Coding"
        }
      }
    },
    "minimax": {
      "options": {
        "baseURL": "https://api.minimaxi.com/anthropic",
        "apiKey": "your-minimax-jwt-token"
      },
      "models": {
        "MiniMax-M2.1": {
          "name": "MiniMax M2.1"
        }
      }
    }
  }
}
```

## 使用

### 1. 设置讨论组

```
/council_setup models=[
  {"providerId": "minimax", "isHost": true},
  {"providerId": "kimi"}
]
```

### 2. 开始讨论

```
/council_discuss topic="如何设计高可用系统？"
```

### 3. 继续下一轮

```
/council_next
```

### 4. 查看状态

```
/council_status includeMessages=true
```

### 5. 结束讨论

```
/council_end generateSummary=true
```

## 可用命令

| 命令 | 描述 |
|---------|-------------|
| `council_setup` | 设置多模型讨论组 |
| `council_discuss` | 使用配置的讨论组开始讨论 |
| `council_status` | 显示当前讨论组状态 |
| `council_models` | 列出可用模型 |
| `council_next` | 进入下一轮讨论 |
| `council_end` | 结束当前讨论 |

## 支持的 Provider

| Provider | 模型 | API 兼容性 |
|----------|--------|-------------------|
| Kimi | kimi-for-coding | Anthropic |
| MiniMax | MiniMax-M2.1 | Anthropic |
| Anthropic | Claude 模型 | 原生 |
| OpenAI | GPT-4o 等 | 原生 |

## 架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     OpenCode TUI                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  /council_setup → /council_discuss → /council_next      │    │
│  │                                                          │    │
│  │  🎤 [主持人: MiniMax] 开场发言...                         │    │
│  │  💬 [Kimi] 我的观点是...                                 │    │
│  │  🎤 [主持人: MiniMax] 总结第一轮...                       │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  AICouncil 插件                                          │    │
│  │  ├── Council (编排)                                      │    │
│  │  ├── ParticipantManager (参与者管理)                     │    │
│  │  ├── RoundManager (轮次管理)                             │    │
│  │  └── ProviderAdapter → OpenCode SDK                      │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

## 开发

```bash
# 安装依赖
cd packages/aicouncil-plugin
bun install

# 构建
bun run build

# 类型检查
bun run typecheck

# 运行测试
bun run test
```

## 许可证

MIT
